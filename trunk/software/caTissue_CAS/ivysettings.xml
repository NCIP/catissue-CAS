<ivysettings>
	<settings defaultResolver="default-resolver" validate="false" />
	<caches default="caTissueCache">
        <cache name="caTissueCache" basedir="${user.home}/.ivy2/caTissue"/>
    </caches>

	<resolvers>
		<chain name="default-resolver" returnFirst="true">

            <filesystem name="localResolver" local="true" >
                  <ivy pattern="${user.home}/.ivy2/cache/[organisation]/[module]/ivy-[revision].xml"/>
                  <artifact pattern="${user.home}/.ivy2/cache/[organisation]/[module]/[type]s/[artifact]-[revision].[ext]"/>
            </filesystem>

            <url name="cbiit-ivy-repo">
                <artifact pattern="https://ncisvn.nci.nih.gov/svn/cbiit-ivy-repo/trunk/[organisation]/[module]/[revision]/[artifact]-[revision].[ext]" />
                <artifact pattern="https://ncisvn.nci.nih.gov/svn/cbiit-ivy-repo/trunk/[organisation]/[module]/[revision]/[artifact][revision].[ext]" />
                <ivy pattern="https://ncisvn.nci.nih.gov/svn/cbiit-ivy-repo/trunk/[organisation]/[module]/[revision]/ivy.xml" />
            </url>

            <ibiblio name="mvn.repo" root="http://repo1.maven.org/maven2/" m2compatible="true" changingPattern=".*SNAPSHOT" />

			<filesystem name="local-repo-jars">
             	<ivy pattern="${ivy.local.jars}/ivy.xml"/>
             	<artifact pattern="${ivy.local.jars}/[artifact].[ext]"/>
        	</filesystem>
        	
        	<url name="NEXUS_SNAPSHOTS" m2compatible="false">
				<artifact pattern="https://ncimvn.nci.nih.gov/nexus/content/repositories/snapshots/[organisation]/[module]/[artifact]/[revision]/[artifact]-[revision].[ext]" />
			</url>

        </chain>

	</resolvers>
</ivysettings>

